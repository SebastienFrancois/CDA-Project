/* eslint-disable */
import AppAuthenticated from './AppAuthenticated';

export default {
  title: "AppAuthenticated",
};

export const Default = () => <AppAuthenticated />;

Default.story = {
  name: 'default',
};
